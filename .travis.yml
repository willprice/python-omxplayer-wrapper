language: python
python:
    - 2.7_with_system_site_packages
cache:
    - pip
    - apt
addons:
  apt:
    packages:
      - python-dbus
      - python3-dbus
install:
    - pip install -r requirements.txt
script: nosetests ./tests
